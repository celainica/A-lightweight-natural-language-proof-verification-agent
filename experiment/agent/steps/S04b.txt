Step ID: S04b

Why this is one minimal logical deduction unit:
This unit keeps only the finite-dimensional linear-algebra consequence of the invariant-subspace identification.

Original step:
Because H_c^*(X;Q) is finite-dimensional, H_c^*(Y;Q) is finite-dimensional, so its compactly supported Euler characteristic chi_c(Y) is a well-defined integer. Algebraically, the dimension of this invariant subspace dictates dim H_c^k(Y;Q)=1/2(dim_Q H_c^k(X;Q)+Tr(gamma^*|H_c^k(X;Q))). Taking the alternating sum over all degrees yields the relation:

chi_c(Y)=1/2(chi_c(X)+L_c(gamma,X)).

Target assertion a_i:
chi_c(Y)=1/2(chi_c(X)+L_c(gamma,X)).

Original route:
Use the dimension formula for the invariant subspace of an involution and then take alternating sums.

Explanation:
Once H_c^*(Y;Q) is identified with the invariant part of H_c^*(X;Q), the rest is ordinary finite-dimensional linear algebra degree by degree.

Statement Extraction `(Gamma_i, Sigma_i, T_i)`:

Assertion:
chi_c(Y)=1/2(chi_c(X)+L_c(gamma,X)).

Local Context `Gamma_i`:
- gamma_1: H_c^*(Y;Q)\cong H_c^*(X;Q)^gamma.
- gamma_2: H_c^*(X;Q) is finite-dimensional.

Global Knowledge `Sigma_i`:
- No nontrivial external statement is used in this unit.

Sigma Support:
- None.

Background Theory `T_i`:
- t_1: For an involution T on a finite-dimensional Q-vector space V, dim V^T=(dim V+Tr(T))/2.
- t_2: The compactly supported Euler characteristic is the alternating sum of the compactly supported Betti numbers when these are finite-dimensional.

Deduction:
From gamma_1 and t_1, for each k,
dim H_c^k(Y;Q)=1/2(dim H_c^k(X;Q)+Tr(gamma^*|H_c^k(X;Q))).
Taking the alternating sum over k and using t_2 gives
chi_c(Y)=1/2(chi_c(X)+L_c(gamma,X)).

Diagnosis:
- conclusion preserved: yes
- route preserved: yes
- exact offending dependence, if any: none
- flaw witness, if any: none

Step Result:
- status: VERIFIED
- main blocker: none
- flaw witness: none
- dependencies: none
- last update: 2026-04-24
