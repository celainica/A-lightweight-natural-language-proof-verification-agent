Step ID: S08

Why this is one minimal logical deduction unit:
This passage is exactly the properness lemma. Its single nontrivial output is that the previously constructed equivariant map f is proper.

Original step:
**Lemma 1.** The Gamma-equivariant map f:X->S is a proper map. *Proof.* Because Gamma acts cocompactly on both X (as M is compact) and S (as Gamma is a uniform lattice), we can select compact fundamental domains D_X subset X and D_S subset S. For any compact subset K subset S, properness of the Gamma-action on S ensures that the set of group elements Phi={g in Gamma | g f(D_X) intersects K != empty} is finite. If x in f^{-1}(K), then x=g.y for some g in Gamma and y in D_X. By Gamma-equivariance, f(x)=g.f(y) in K, enforcing g in Phi. Thus, the closed set f^{-1}(K) is contained entirely in the finite union of compact sets union_{g in Phi} g.D_X. Therefore, f^{-1}(K) is definitively compact, establishing that f is a proper map.

Target assertion a_i:
The Gamma-equivariant map f:X->S is proper.

Original route:
Use cocompactness to trap every point of X in a Gamma-translate of a compact set, then use properness of the Gamma-action on S to show only finitely many translates can meet a fixed compact subset of S.

Explanation:
Choose compact subsets D_X subset X and D_S subset S whose Gamma-translates cover X and S. Let K subset S be compact. Properness of the Gamma-action on S implies that only finitely many translates g f(D_X) can intersect K; call the finite set of such group elements Phi. Every x in f^{-1}(K) can be written as g y with y in D_X. Equivariance gives f(x)=g f(y), so g belongs to Phi. Hence f^{-1}(K) lies inside the finite union union_{g in Phi} g D_X. Each g D_X is compact, so the union is compact. Because f^{-1}(K) is closed as the preimage of a closed set under a continuous map, it is a closed subset of a compact set, hence compact.

Statement Extraction `(Gamma_i, Sigma_i, T_i)`:

Assertion:
For every compact K subset S, f^{-1}(K) is compact.

Local Context `Gamma_i`:
- gamma_1: f:X->S is continuous and Gamma-equivariant.
- gamma_2: Gamma acts cocompactly on X.
- gamma_3: Gamma acts cocompactly and properly on S.

Global Knowledge `Sigma_i`:
- No nontrivial external statement is newly used in this unit.

Sigma Support:
- None.

Background Theory `T_i`:
- t_1: Cocompactness means there exists a compact subset whose Gamma-translates cover the space.
- t_2: For a proper action of a discrete group on S, only finitely many group elements can move a fixed compact subset to meet another fixed compact subset.
- t_3: A closed subset of a compact set is compact.

Deduction:
Fix compact K subset S. By gamma_2 choose compact D_X with Gamma D_X = X. By gamma_3, applied to the compact sets f(D_X) and K, the set Phi={g in Gamma | g f(D_X) intersects K != empty} is finite. If x in f^{-1}(K), write x = g y with y in D_X. Then f(x)=g f(y) belongs to K, so g belongs to Phi. Hence f^{-1}(K) subset union_{g in Phi} g D_X. The right-hand side is a finite union of compact sets and therefore compact. Since f^{-1}(K) is closed, t_3 shows it is compact. Thus f is proper.

Diagnosis:
- conclusion preserved: yes
- route preserved: yes
- exact offending dependence, if any: none
- flaw witness, if any: none

Step Result:
- status: VERIFIED
- main blocker: none
- flaw witness: none
- dependencies: none
- last update: 2026-04-24
