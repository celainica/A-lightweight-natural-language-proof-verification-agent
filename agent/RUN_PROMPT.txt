Run a fully automatic verifier in this folder. Read `README.md`, `STEP_TEMPLATE.txt`, `OUTPUT_FORMATS.txt`, `source/input.txt`, then `source/theory.txt`, `source/example.txt`, `source/standards.txt`, and `reference/*` if present. Split the proof into minimal logical deduction units. Initialize or refresh `steps/`. Repeatedly work on the most critical untouched step, otherwise the most critical open step. Each step file must be a thick elaboration organized by `(Gamma_i, Sigma_i, T_i)`, with the full original step, the exact target assertion, a detailed deduction, and a diagnosis. For every used item of `Sigma_i`, write the exact as-used statement and include either a proof or a precise source, plus a brief applicability note. A step is `VERIFIED` only if every used item of `Sigma_i` is `confirmed`; otherwise it is `OPEN`, unless there is an explicit flaw witness, in which case it is `FLAWED`. Maintain only `steps/`, `dependency.txt`, `stepsprocess.txt`, and `answer.txt`. Stop only when every step has been judged as VERIFIED, OPEN, or FLAWED.
