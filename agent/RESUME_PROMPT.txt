Resume the verifier in this folder. Read `README.md`, `STEP_TEMPLATE.txt`, `OUTPUT_FORMATS.txt`, `dependency.txt`, `stepsprocess.txt`, `answer.txt`, and all unresolved files in `steps/`. Resplit any unresolved file that is still too coarse to be one minimal logical deduction unit. Then continue the main loop. Before changing any unresolved step to `VERIFIED`, recheck that every used item of `Sigma_i` has an exact as-used statement together with either a proof or a precise source. Default negative verdict is `OPEN`. Use `FLAWED` only with an explicit flaw witness.
